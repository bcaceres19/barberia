---
name: nava-mockup-fidelity
description: "Alias heredado de visual-qa en modo fidelidad. Se conserva para que los prompts persistentes que citan esta ruta sigan resolviendo; no se activa automáticamente."
disable-model-invocation: true
---

# nava-mockup-fidelity · alias heredado

Este skill fue absorbido por el skill compartido `visual-qa` (issue #259, `DEC-086`). Su contenido ya no se mantiene aquí.

Si un issue, un prompt o el usuario te remitió a este archivo:

1. Lee completo `.agents/skills/visual-qa/SKILL.md` y aplica su **modo fidelidad**, antes de editar y antes de declarar la pantalla terminada.
2. Usa las tablas de `.agents/skills/visual-qa/references/fidelity-gates.md` (Gates A–D y tolerancias).
3. Demuestra el viewport efectivo con `.agents/skills/visual-qa/references/viewport-verification.md`.
