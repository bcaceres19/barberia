---
name: visual-qa
description: "Verify a rendered NAVA interface with screenshots and interaction evidence, in identity-guided mode or against an assigned exact mockup, screenshot, or Figma frame. Use after visible UI changes, for critique grounded in the running app, for responsive/viewport checks (320/360/768/1280 px, resize_window), and before editing and before declaring done whenever a screen must match a reference. Do not use for code-only review or speculative critique without a renderable target."
---

# visual-qa · adaptador de Claude Code

Este archivo solo expone el skill a Claude Code. Su fuente canónica, compartida con Codex, es `.agents/skills/visual-qa/SKILL.md`.

1. Lee completo `.agents/skills/visual-qa/SKILL.md` antes de actuar y síguelo como si fuera este archivo.
2. Resuelve sus enlaces relativos (`references/...`) desde `.agents/skills/visual-qa/` y cárgalos solo cuando el skill lo indique.
3. No edites el contenido aquí: cambia el canónico y ejecuta `tools/ai/validate-agent-system.sh`.
