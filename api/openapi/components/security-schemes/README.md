# components/security-schemes

Esquemas de seguridad de las tres audiencias (pública, cliente por token
del enlace, sesión privada del barbero). El mecanismo exacto de sesión
(`F-AUTH-01`) todavía no tiene una decisión `DEC-*` registrada; no se
inventa aquí, según `AGENTS.md`. Sin contenido todavía.
