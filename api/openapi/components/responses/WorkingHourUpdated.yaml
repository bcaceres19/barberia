# Respuesta 200 de PATCH
# /private/barbers/{barberId}/working-hours/{workingHourId} (HU-040,
# CA-040-04): intervalo completo ya guardado, mismo `id`, solo cambian los
# campos del intervalo y `updatedAt`.
description: Tramo con el intervalo ya actualizado.
headers:
  X-Request-Id:
    $ref: '../headers/XRequestId.yaml'
content:
  application/json:
    schema:
      $ref: '../schemas/WorkingHourResponse.yaml'
    examples:
      tramo:
        value:
          id: 6f1a2b3c-4d5e-4f60-8172-8394a5b6c7d8
          isoWeekday: 1
          startsTime: "09:00"
          durationMinutes: 420
          createdAt: "2026-08-25T15:04:05Z"
          updatedAt: "2026-08-25T16:10:00Z"
